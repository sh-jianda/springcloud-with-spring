server:
  port: 8003
spring:
  application:
    name: order-server
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://127.0.0.1:8001/eureka/
    fetch-registry: true
  instance:
    prefer-ip-address: true
